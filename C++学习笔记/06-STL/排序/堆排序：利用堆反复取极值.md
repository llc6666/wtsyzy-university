---
title: 堆排序
tags:
  - c-plus-plus
  - algorithm
  - sorting
created: 2026-09-01
---

# 堆排序

把数组组织成堆，每轮取出最大值或最小值。它能保证 `O(n log n)`，额外空间较小，但实现复杂、通常不稳定。需要“动态维护极值”时，往往更适合直接使用 `std::priority_queue`，而不是手写完整堆排序。

## 关联

排序总览：算法选择与复杂度
